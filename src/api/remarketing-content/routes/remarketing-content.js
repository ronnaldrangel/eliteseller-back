'use strict';

/**
 * remarketing-content router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::remarketing-content.remarketing-content');
