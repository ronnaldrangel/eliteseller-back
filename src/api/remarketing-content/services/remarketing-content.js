'use strict';

/**
 * remarketing-content service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::remarketing-content.remarketing-content');
